{% extends 'base.html.twig' %}
{% block title %}
{{ parent()}} Liste des utilisateurs {% endblock %}{% block contenu %}  
    <h1> Liste des utilisateurs </h1>  
{% if not listeUser is empty %} 
    <table class="table">   
        <tr><th>Nom</th><th>Prénom</th></tr>  
    {% for key,value in listeUser %} 
            <tr><td><a href="">{{ value.nom }}</a>
                </td><td>{{ value.prenom }}</td></tr> 
    {% endfor %}   
    </table>
{% else %}  
    Aucun utilisateur à afficher
{% endif %}
{% endblock %}